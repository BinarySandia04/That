#include "trig.h"

#include <cmath>

double tstd::MathCos(double x){
  return std::cos(x);
}

double tstd::MathSin(double x){
  return std::sin(x);
}

double tstd::MathTan(double x){
  return std::tan(x);
}
