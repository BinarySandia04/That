[info]
name = "math"
version = "0.1v"
namespace = "zagmath"
root = "math"

[root.sin]
bind_name = "zsin"
bind_deps = ["src/trig.cpp"]

[root.cos]
bind_name = "zcos"
bind_deps = ["src/trig.cpp"]

[root.tan]
bind_name = "ztan"
bind_deps = ["src/trig.cpp"]

